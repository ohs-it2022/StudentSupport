import 'package:flutter/material.dart';

class AttendanceInfoWidget extends StatelessWidget {
  const AttendanceInfoWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('出欠情報画面')),
    );
  }
}