import 'package:flutter/material.dart';

class JobRegist extends StatelessWidget {
  const JobRegist({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('課題登録画面')),
    );
  }
}